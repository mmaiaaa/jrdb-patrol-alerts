Outputs

Large raw predictions, local environment reports, and visual-debug batches belong in the ignored `local/`, `raw/`, `predictions/`, or `visuals/` subdirectories.

Create selected `tables/` and `figures/` when actual results exist. These smaller artifacts can be committed with their run IDs and generation instructions after checking applicable data-sharing terms. There are no results in the foundation commit.

Record failures as well as successful outputs. Preserve enough counts and exposure in `manifests/event_results.csv` to recompute pooled rates; a screenshot of a terminal is not the sole research record.
