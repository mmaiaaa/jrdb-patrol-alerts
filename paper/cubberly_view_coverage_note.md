# Cubberly visual coverage check (development; draft)

The 80-frame five-camera detector cache is complete: 400 camera-frame rows, with 245/0/444/449/171 person **boxes** in cameras 0/2/4/6/8. The zero-box frame counts are 0/80/0/0/18, respectively. Counts are box-level outputs at a 0.1 confidence cutoff, not independent visible people or detector recall. The initial same-camera IoU ≥ 0.5 audit reported 39/0/88/68/41 box pairs; their identities and correctness remain unverified.

For frame `000087`, visual inspection of the supplied five-view sheet showed an unobstructed wall in camera 2, with no clearly visible person there. Cameras 0, 4 and 6 show people and detector boxes in their own views. Camera 8 includes what appears to be part of a person at the extreme right edge, despite reporting zero boxes. This is one subjective image-level inspection; it does not prove that all 80 camera-2 views are empty, or that a person was evaluable in camera 8. No ground-truth matching was performed.

`scripts/audit_jrdb_native_view_coverage.py` makes four contact sheets for the 80 zero-box camera-2 frames and one for the 18 zero-box camera-8 frames. These images are only a triage aid. Review each page and open full-resolution original JPEGs when a figure could be a small or partly cut-off person. Record ambiguous frames explicitly instead of counting them as negatives. In later evaluations, person visibility and the relevance of a particular camera must be defined independently of detector output and linked to a track and event protocol.

Do not drop camera 2 from the study based on this single short development segment. Its zero boxes may reflect the recording's view direction. The inference run includes all five cameras; later camera-selection decisions should follow a prespecified protocol across development locations and preserve a fair compute comparison. Nothing in this check establishes LiDAR distance, elapsed capture time, alert performance, or an equipment-upgrade requirement.
